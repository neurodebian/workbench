--- a/src/kloewe/dot/CMakeLists.txt
+++ b/src/kloewe/dot/CMakeLists.txt
@@ -10,6 +10,10 @@ endif()
 
 add_compile_options(-std=c99 -Wall -Wextra -Wno-unused-parameter -Wconversion -Wshadow -pedantic)
 
+# Debian: since for some reason exporting DEB_CFLAGS_MAINT_APPEND in debian/rules
+# was not in affect for this one
+add_compile_options(-DNDEBUG)
+
 SET(DOT_USEFMA 0)
 SET(DOT_USEAVX512 0)
 
